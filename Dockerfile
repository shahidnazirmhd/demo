# Use the official pgAdmin image as the base
FROM dpage/pgadmin4:latest

# (Optional) You can add extra setup or copy config files here if needed

# Default command is already set in the base image, so no need to override
